{
  "name": "dos-hermanos-firestore-rules-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --project demo-dos-hermanos --only firestore \"node --test *.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.17.1",
    "firebase-tools": "15.28.1"
  }
}
